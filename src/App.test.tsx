import { describe, expect, it } from 'vitest';
import App from './App';

describe('App', () => {
  it('is a component (placeholder so CI has something to run)', () => {
    expect(typeof App).toBe('function');
  });
});
